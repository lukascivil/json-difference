{
  "preset": "ts-jest",
  "testMatch": ["**/*.spec.ts"],
  "modulePathIgnorePatterns": ["<rootDir>/dist"],
  "transform": {
    "^.+\\.tsx?$": ["ts-jest", { "tsconfig": "<rootDir>/tsconfig.spec.json" }]
  }
}
